import csv
from collections import defaultdict


data = defaultdict(list)

for i, row in enumerate(csv.reader(open('/home/ajay/cid.csv', 'r'))):
    if not i or not row:
        continue
        d=data['Published by'].append(float(['Total enrolments By State']))
        print(d)
        x='Collection id '
        for x in data.iteritems():
            print(sum('Total enrolments By State'))
